1.git fetch <远程主机名> <远程分支名>:<本地分支名>
**这个时候，因为指定了本地分支的名字，所以就和在哪个分支下不相关，git merge 的时候就是FETCH_HEAD与<本地分支名>进行merge
2.git fetch <远程主机名> <远程分支名>
**关键是，在那个分支下使用git fetch<远程主机名> <远程分支名>的时候，就会在当前分支下返回一个FETCH_HEAD，然后git merge FETCH_HEAD的时候就是与当前分支进行合并，而不是与和远程分支名相同的分支进行merge
